const expect = require('chai').expect;
var request = require('request');

const server = require('../../server/server.js');
const utils = require('../../server/config/utils.js');
const db = require('../../server/db/index.js');


it('Signup logs in a new user', () => {

});

it('Signin logs in an existing, unauthenticated user', () => {

});

it('Logs in an existing, authenticated user automatically', () => {

});

it('Should accept posts to /destinations', () => {

});

it('Should accept posts to /trains', () => {

});

it('Returns all of the trains to display on the trains page', () => {

});

it('Returns all of the destinations to display on the destinations page', () => {

});

it('New destinations create a database entry', () => {

});

it('New trains create a database entry', () => {

});